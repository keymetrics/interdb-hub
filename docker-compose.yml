version: '3'

services:
  redis:
    image: redis
    ports:
      - "6379:6379"
  interdb-hub:
    build:
      context: .
    links:
      - redis
    environment:
      - HUB_REDIS_HOST=redis
    ports:
      - "12000:80"